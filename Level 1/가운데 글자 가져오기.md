# 가운데 글자 가져오기

```java
class Solution {
    public String solution(String s) {
        String answer = "";
        return String answer = s.length() % 2 == 0 ? answer + s.substring(s.length()/2 - 1, s.length()/2 + 1): answer + s.substring(s.length()/2, s.length()/2 + 1);
    }
}
```